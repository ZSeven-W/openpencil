{
  // Must match the ohpm modelVersion in /oh-package.json5; with a
  // modelVersion the IDE resolves the hvigor and hvigor-ohos-plugin
  // versions itself (declaring hvigorVersion here would conflict).
  "modelVersion": "5.0.0",
  "dependencies": {},
  "execution": {},
  "logging": {},
  "debugging": {},
  "nodeOptions": {}
}
