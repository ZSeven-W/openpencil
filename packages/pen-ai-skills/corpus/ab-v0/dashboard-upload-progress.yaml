id: dashboard-upload-progress
category: dashboard
difficulty: obvious
prompt: |
  Design ONLY a linear horizontal progress bar showing roughly 60%
  complete. No surrounding card, no title, no subtitle, no
  cancel button — just the progress bar itself.
expected:
  must_contain_roles:
    - progress-bar
    - progress-bar-fill
expected_tool_if_any: add_progress_bar_v0
