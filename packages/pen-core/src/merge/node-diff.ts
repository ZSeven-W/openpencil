// packages/pen-core/src/merge/node-diff.ts
//
// One-direction diff: compute the patches needed to transform `base` into `next`.
// Used by the git engine to summarize what a commit changed (for autosave commit
// messages and the diff view) and as a building block by node-merge.ts.
//
// Filled in by Task 5.

export {};
