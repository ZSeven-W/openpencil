[package]
name = "op-host-web-server"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "OpenPencil headless web/MCP server binary — links op-web-daemon only, no winit/skia-GL"

[[bin]]
name = "op-host-web-server"
path = "src/main.rs"

[dependencies]
op-web-daemon = { path = "../op-web-daemon" }
